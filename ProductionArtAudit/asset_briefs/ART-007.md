# ART-007 — Shell UI panel (9-slice)

## 1. Purpose
9-slice panel/card chrome replacing the flat dark scaffold rectangles on every shell panel.

## 2. Launch priority
LAUNCH_REQUIRED / P1

## 3. Current problem
PLACEHOLDER: all shell panels/cards use Unity built-in UISprite tinted (0.15,0.15,0.2) (problem: PLACEHOLDER_REPLACEMENT)

## 4. Evidence
scaffold sources (BuildGameShellCanvas.cs:132 etc.) tint built-in sprite; flat rects in all review shots (labels: STATIC-INSPECTED;RUNTIME-OBSERVED)

## 5. Exact target file and archive path
`Assets/_JebbyJump/Art/Sprites/UI/ui_panel_soft_9s.png` (target dir `Assets/_JebbyJump/Art/Sprites/UI/`, filename `ui_panel_soft_9s.png`)

## 6. Replacement / .meta / GUID handling
NEW_ASSET_REQUIRES_WIRING. Never include any .meta in the ZIP. Existing meta note: new file.

## 7. Locked references
- Art/Sprites/UI/ui_btn_jump.png (ornament style anchor - upload)

## 8. Visual design contract
Rounded soft panel: deep blue-charcoal body (#262633 family) with a thin warm gold rim and very soft inner glow, corners uniformly rounded (candidate radius 48px on a 256 canvas - OPEN decision), all four stretch regions PERFECTLY flat so 9-slice scaling never distorts. Subtle, not ornate - content sits on it.

## 9. Exact pixel canvas
Runtime: **256x256px**; master: 512x512. Colour space sRGB, 32-bit. Format PNG.

## 10. Exact content bounds and transparent padding
Full-canvas composition; corners transparent where alpha applies.

## 11. Pivot, baseline and contact points
Not applicable (UI/marketing image).

## 12. Unity import settings
spriteMode: Single (9-sliced); PPU: 100; 9-slice border: BLOCKED - derive from generated corner radius; candidate 64px all sides on 256 canvas

## 13. Scene/prefab placement
used_by_scenes: MainMenu.unity;Game.unity; image_type: Sliced

## 14. Colour and rendering specification
deep neutral #262633 body + light border, per Art Bible rounded/soft

## 15. Animation specification
Not applicable (single static image)

## 16. Accessibility requirements
Must preserve >=90 canvas-unit touch targets and text contrast (P20/P21 tested metrics); focus ring stays visible on top.

## 17. ChatGPT generation feasibility
VECTOR_OR_CODE_GENERATION_PREFERRED (reference image required: yes; human review: no)

## 18. ChatGPT generation prompt
Create a 512x512 UI panel background (downscaled to exactly 256x256) for 9-slice use: rounded rectangle filling the canvas minus 8px margin, body deep blue-charcoal #262633 at ~97% opacity feel (render opaque; alpha only outside the shape), a 3px warm gold (#E6C36B) rim, faint soft inner top-light. Corners identical, radius exactly 96px on this 512 canvas. The middle of every edge and the centre must be COMPLETELY flat colour (no texture/gradient) for slicing. Output: PNG RGBA. Filename: ui_panel_soft_9s.png

## 19. Negative prompt / forbidden changes
No texture/noise/gradient in stretch regions. No decorations except the rim. No drop shadow (Unity handles depth). No text.

## 20. Deterministic post-processing instructions
Downscale to 256x256; assert edge-middle rows/columns are uniform colour; assert 4-corner alpha 0; record final border px for the Sprite Editor (expected 64).

## 21. Unity integration steps
Sprite Editor: border L/R/T/B = 64 (from radius+rim). Swap Image sprite + set Image.type=Sliced + colour=white on: MainMenu panels (3) + level cards + Game shell panels (4) + wardrobe card/ceremony. Multiple scene/prefab edits; rerun overlap tests.

## 22. Acceptance checklist
- [ ] flat stretch regions (validator)
- [ ] uniform corners
- [ ] text contrast >= current dark rects

## 23. Blockers/open decisions
Corner-radius system is an OPEN_SCOPE_DECISION (candidate 48px@256 documented).
