# ART-008 — Shell UI pill button (9-slice)

## 1. Purpose
9-slice pill button chrome for all shell buttons (currently flat dark rects; Art Bible demands pills).

## 2. Launch priority
LAUNCH_REQUIRED / P1

## 3. Current problem
PLACEHOLDER: built-in UISprite tinted (0.2,0.2,0.25) (problem: PLACEHOLDER_REPLACEMENT)

## 4. Evidence
scaffold tints; flat rect buttons in all shell screenshots; Art Bible requires pill-style (labels: STATIC-INSPECTED;RUNTIME-OBSERVED)

## 5. Exact target file and archive path
`Assets/_JebbyJump/Art/Sprites/UI/ui_btn_pill_9s.png` (target dir `Assets/_JebbyJump/Art/Sprites/UI/`, filename `ui_btn_pill_9s.png`)

## 6. Replacement / .meta / GUID handling
NEW_ASSET_REQUIRES_WIRING. Never include any .meta in the ZIP. Existing meta note: new file.

## 7. Locked references
- Art/Sprites/UI/ui_btn_jump.png (style anchor - upload)
- ART-007 output (family)

## 8. Visual design contract
True pill (corner radius = half height), slightly lighter than the panel (#333340 family) with the same thin gold rim; drawn NEUTRAL-bright enough that Unity tint states (normal/highlighted/pressed/disabled) read; ONE bitmap only - no per-state art. Focus ring stays Unity-procedural.

## 9. Exact pixel canvas
Runtime: **256x96px**; master: 512x192. Colour space sRGB, 32-bit. Format PNG.

## 10. Exact content bounds and transparent padding
Full-canvas composition; corners transparent where alpha applies.

## 11. Pivot, baseline and contact points
Not applicable (UI/marketing image).

## 12. Unity import settings
spriteMode: Single (9-sliced); 9-slice border: left/right 48 top/bottom 40 on 256x96 (candidate; pill =半 height radius)

## 13. Scene/prefab placement
used_by_scenes: MainMenu.unity;Game.unity; image_type: Sliced; minimum_touch_target: 90 canvas units (ShellLayoutMetrics single-source)

## 14. Colour and rendering specification
state via Unity tint (Normal white base art; pressed/disabled tints) - single bitmap

## 15. Animation specification
Not applicable (single static image)

## 16. Accessibility requirements
Must preserve >=90 canvas-unit touch targets and text contrast (P20/P21 tested metrics); focus ring stays visible on top.

## 17. ChatGPT generation feasibility
VECTOR_OR_CODE_GENERATION_PREFERRED (reference image required: yes; human review: no)

## 18. ChatGPT generation prompt
Create a 512x192 UI button (downscaled to exactly 256x96) for 9-slice use: a horizontal pill (corner radius exactly 96px on this canvas = fully rounded ends), body #333340, 3px warm gold rim (#E6C36B), soft top highlight inside the rim. Ends identical; the horizontal middle 50% must be COMPLETELY flat for stretching. Alpha 0 outside the pill. Output: PNG RGBA. Filename: ui_btn_pill_9s.png

## 19. Negative prompt / forbidden changes
No text/icon. No gradient in the stretch band. No outer shadow. No square corners.

## 20. Deterministic post-processing instructions
Downscale 256x96; flat-band check; corner symmetry check; record borders (48/48/40/40).

## 21. Unity integration steps
Sprite Editor borders 48/48/40/40; swap Button Image sprites across MainMenu stack (5), panel Back/Reset/Equip buttons, Game result/pause buttons; keep >=90u hit areas (ShellLayoutMetrics tests) + tint transitions; rerun shell tests.

## 22. Acceptance checklist
- [ ] pill silhouette; flat stretch band
- [ ] tint states legible
- [ ] >=90u touch targets preserved

## 23. Blockers/open decisions
Shares the radius OPEN decision with ART-007 (pill is self-consistent regardless).
