# Jebby Jump — Store Listing Copy (DRAFT) v0.1 (P31)

> **STATUS: DRAFT — USER APPROVAL REQUIRED — NOT SUBMITTED.** Not uploaded to Play; not
> legal advice; no Google approval implied. `<PROVIDE: …>` marks external values the user
> must supply; any section containing a placeholder is **Blocked** and cannot pass final
> readiness.

## Listing copy (draft — within Play limits; no unverified claims)

- **Title** (≤30): `Jebby Jump`
- **Short description** (≤80): `Memory platformer: watch the color sequence, then hop the matching tiles.`
- **Full description** (≤4000):

  `Jebby Jump is a colorful memory platformer. Each level shows a short color sequence;
  remember it, then jump across the matching platforms before the clock runs out. Chase
  faster clear times and time ranks, and dress Jebby up with cosmetic outfits. Designed
  mobile-first for landscape play.`

- **Category (recommendation):** Games → Arcade or Puzzle (user chooses)
- **Tags / keywords:** memory, platformer, casual, color, family
- **Release notes (internal):** `Internal testing build — core memory/platforming loop,
  10 levels, time ranks, cosmetic wardrobe. Prototype art; balance provisional.`
- **Support-contact copy:** `Questions or feedback? Contact <PROVIDE: support email>.`

## Claims guardrails (enforced)

No misleading claims; **no** "final/best/certified/guaranteed" balance-or-art claims; **no**
"for kids" claim beyond the declared Families audience; **no** performance/accessibility
certification claims; **no** monetization claims (the build wires no ads/IAP). Checked by
`ListingCopyWithinLimits` + `ListingCopyHasUnverifiedClaim`.

## Evidence / sources

- Identity/audience: P26 `Store_Submission_Readiness` + P27 `Play_Declaration_Record`
  (`com.sparklibrary.jebbyjump`, Mixed → Families).
- Feature claims: actual implemented systems (memory sequence, 10 levels, time ranks,
  cosmetic wardrobe) — no unimplemented features described.
- Limits/claims: `DistributionDeclarationPolicy` (Title 30 / short 80 / full 4000; no
  unverified-claim terms).

## Approvals still required

Final wording approval by the user; real support email (replaces `<PROVIDE>`); category +
tag confirmation.
